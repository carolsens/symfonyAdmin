{% extends 'admin/base.html.twig' %}

{% block title %}Banner{% endblock %}

{% block body %}
    <h1>Banner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ banner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ banner.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ banner.image }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ banner.position }}</td>
            </tr>
            <tr>
                <th>Sequence</th>
                <td>{{ banner.sequence }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ banner.status }}</td>
            </tr>
            <tr>
                <th>Deleted_at</th>
                <td>{{ banner.deletedAt ? banner.deletedAt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_banner_index') }}">back to list</a>

    <a href="{{ path('app_banner_edit', {'id': banner.id}) }}">edit</a>

    {{ include('admin/banner/_delete_form.html.twig') }}
{% endblock %}
